# Local sharing mode archive

Local network sharing mode is archived and experimental. DMCC is web-first: collaboration should use account sessions, campaign workspaces, and web invitations backed by PostgreSQL/Neon.

Do not add new local-network routes, local tokens, or machine-local authentication flows unless this mode is explicitly reactivated. Historical compatibility routes should return gone responses instead of reviving local behavior.
